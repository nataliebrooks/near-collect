specVersion: 0.0.4
description: near collect common-good
repository: https://github.com/elliotBraem/near-frontier/tree/main/subgraphs
schema:
  file: schema.graphql
dataSources:
  - kind: near
    name: Token
    network: near-testnet
    source:
      account: common-good.collect.testnet
      startBlock: 81834011
    mapping:
      apiVersion: 0.0.5
      language: wasm/assemblyscript
      file: Token/Token.wasm
      entities:
        - Token
        - User
      receiptHandlers:
        - handler: handleReceipt
